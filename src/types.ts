export interface ActivityItem {
  id: string;
  title: string;
  description: string;
  iconName: string; // We'll map this to a LucideIcon
}

export interface BenefitItem {
  id: string;
  title: string;
  description: string;
}

export interface PillarItem {
  id: string;
  title: string;
  iconName: string;
}
